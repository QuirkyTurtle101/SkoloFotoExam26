@page
@using SkoloFotoExam26.Models
@model SkoloFotoExam26.Pages.Bookings.CreateBookingModel
@{
    ViewData["Title"] = "Create Booking";
}
<h1>
    Create booking
</h1>

<div class="form-group">
</div>
ID: TheEvent.PhotographingEventID
Start: TheEvent.Start
End: TheEvent.End
Photographer: TheEvent.Photographer.FirstName TheEvent.Photographer.LastName
<br />

<form method="post">
    <div>
        <label asp-for="Start" class="control-label"></label>
        <input asp-for="Start" class="form-control"/>
    </div>
    <div>
        <label asp-for="End" class="control-label"></label>
        <input asp-for="End" class="form-control" />
    </div>
    <div class="form-group">
        <label asp-for="TeacherID"> Choose a Teacher </label>
        <select asp-for="TeacherID" class="form-select">
            <option value=""> Select Teacher</option>
            @foreach (Teacher t in @Model.TeacherList)
            {
                <option value="@t.TeacherID">
                    @t.FirstName @t.LastName
                </option>
            }
        </select>
    </div>
    <div class="form-group">
        <label asp-for="SchoolClassID"> Choose a School Class </label>
        <select asp-for="SchoolClassID" class="form-select">
            <option value=""> Select Teacher</option>
            @foreach (SchoolClass s in @Model.SchoolClassList)
            {
                <option value="@s.SchoolClassID">
                    @s.ClassName
                </option>
            }
        </select>
    </div>



</form>